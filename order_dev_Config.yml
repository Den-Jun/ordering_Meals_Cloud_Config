server:
  port: 8010

#eureka配置
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/ #注册到那个注册中心地址
    instance:
    instance-id: Order_provider #自定义名字
    prefer-ip-address: true #访问路径显示ip